{% extends "Admin/admin-layout.njk" %}


{% block content %}
<section>
    <div id="form_head">
        <h2>Receita: {{recipe.title}}</h2>
        <div>
            <a href="/admin/recipes/{{ recipe.id }}/edit">
                <button 
                    type="submit" 
                    form="recipe_form" 
                    class="btn" id="save"                    
                    >                    
                    Editar receita
                </button>
            </a> 
        </div>
    </div>

    <div class="recipe">
        <div class="recipe-header">
            <img src="{{recipe.image}}" alt="{{recipe.title}}">
            <h1>{{recipe.title}}</h1>
            <h2>por {{recipe.chef_name}}</h2>
        </div>

        <div class="recipe-content">
            <div class="topic">
                <div class="recipe-item">
                    <h3>Ingredientes</h3>                
                </div>
                <div class="topic-content" id="ingredients">
                    <ul>
                        {% for ingredient in recipe.ingredients %}
                        <li>- {{ingredient}}</li>
                        {% endfor %}
                    </ul>
                </div>
            </div>

            <div class="topic">
                <div class="recipe-item">
                    <h3>Modo de preparo</h3>
                </div>            
                <div class="topic-content" id="preparation">
                    <ul>
                        {% for preparation in recipe.preparation %}
                        <li>- {{preparation}}</li>
                        {% endfor %}
                    </ul>
                </div>
            </div>

            <div class="topic">
                <div class="recipe-item">
                    <h3>Informações adicionais</h3>
                </div>            
                <div class="topic-content" id="info">
                    <p>{{recipe.information|nl2br}}</p>
                </div>
            </div>
        </div>
    </div>
</section>

    <script src="/scripts.js"></script>
{% endblock content %} 